{
  "name": "my-bot-store-clean",
  "version": "9.0.0",
  "private": true,
  "description": "Telegram digital store v9 with one-click partner bootstrap variables, isolated PostgreSQL schemas, shared catalog API, payment fallback, settlements, partial-wallet checkout, and delivery recovery IDs",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/bot.js && node --check src/db.js && node --check src/network.js && node --check src/utils.js && node --check src/translator.js && node --check src/services/orders.js && node --check src/services/referrals.js && node --check src/payments/binancePay.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "node-telegram-bot-api": "^0.66.0",
    "pg": "^8.16.0",
    "sequelize": "^6.37.7"
  }
}

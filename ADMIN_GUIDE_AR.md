# دليل الأدمن — CD Store v15

هذا الإصدار يرتب لوحة الإدارة حول قاعدة واضحة: **كل عملية مهمة لها إضافة/إزالة أو إظهار/إخفاء أو تشغيل/إيقاف عندما يكون العكس منطقياً**. كل أدمن تتم إضافته من داخل البوت يحصل على صلاحيات الإدارة الكاملة في ذلك البوت، وتُحفظ الصلاحية في PostgreSQL.

> في v13 زر **حذف** يعني حذفاً نهائياً فعلياً، وليس أرشفة. إذا تريد إيقاف المنتج مؤقتاً استخدم **إخفاء** من قسم النشر والظهور. حذف عنصر مخزون يبقى مسموحاً فقط إذا لم يُبع أو يُستخدم.

## 0) جمناي 18 شهر — المخزون والخصم الذكي

بعد تشغيل v14 في البوت الرئيسي يُنشأ منتج **جمناي 18 شهر** مرة واحدة إذا لم يكن موجوداً. السعر الأساسي للزبون هو **$0.50**.

### خطة الخصم

لتجنب التداخل عند 10 و50 و100 و500، يعتمد النظام الحدود التالية:

- 1–9: **$0.50** للوحدة.
- 10–49: **$0.40** للوحدة.
- 50–99: **$0.35** للوحدة.
- 100–499: **$0.30** للوحدة.
- 500–1000: **$0.25** للوحدة.

الخصم يُحسب من **كمية الطلب**، لكن لا يُعرض للزبون إلا إذا كان المخزون الحالي يسمح بالوصول إلى ذلك المستوى. إذا المخزون 8 مثلاً لا تظهر أي خصومات. إذا المخزون 70 يظهر مستوى 10 ومستوى 50 فقط.

### ربح البوتات الشريكة

في البوتات الأخرى لا تعمل خصومات الكمية. السعر الافتراضي للزبون **$0.50**، بينما حق البوت الرئيسي من المخزون **$0.40** لكل رابط. النتيجة الافتراضية: **$0.10 ربح للشريك لكل عملية بيع**. إذا رفع الشريك السعر إلى $0.70 مثلاً، يبقى حق المصدر $0.40 ويصبح ربح الشريك $0.30.

### ربط قناة المخزون

1. افتح `/admin` → **إدارة المنتجات** → **إدارة المنتجات المضافة** → **جمناي 18 شهر**.
2. اضغط **📡 قناة المخزون التلقائي**.
3. اضغط **ربط قناة المخزون**.
4. أسهل طريقة: اعمل Forward لأي رسالة من القناة إلى البوت. ويمكن أيضاً إرسال `@username` للقناة العامة أو Channel ID يبدأ بـ `-100`.
5. أضف البوت Admin في القناة وفعّل صلاحية **Delete Messages**.
6. استخدم **🧪 فحص صلاحية القناة** للتأكد.

بعد ذلك، أي منشور جديد يحتوي رابط `serviceactivation.google.com/subscription/new/` يُضاف تلقائياً إلى مخزون جمناي 18 شهر. بعد نجاح الحفظ يحذف البوت منشور الرابط من القناة. الروابط المكررة لا تدخل مرتين. إذا فشل الحفظ، المنشور يبقى في القناة.

> ملاحظة: Telegram Bot API لا يتيح للبوت مسح تاريخ القناة القديم. الالتقاط التلقائي يبدأ من المنشورات الجديدة بعد إضافة البوت للقناة وبدء الاستقبال.


## 1) لوحة المنتجات الجديدة

من `/admin` افتح **إدارة المنتجات**. ستجد فقط:

1. **➕ إضافة منتج**
2. **إدارة المنتجات المضافة**
3. **🔎 استرجاع منتج / طلب**

تم إلغاء الأزرار المكررة مثل «إضافة خدمة محلية» و«إضافة مخزون» من الصفحة الرئيسية. إضافة المخزون أصبحت داخل المنتج نفسه، والخدمة أصبحت نوعاً من أنواع «إضافة منتج».

## 2) إضافة منتج خطوة بخطوة

اضغط **➕ إضافة منتج**.

### الخطوة الأولى — مكان النشر

- **🔒 محلي — داخل هذا البوت فقط**: لا يصل لبقية البوتات.
- **🌐 عام — لكل البوتات بعد الموافقة**: يدخل الشبكة، ويصل إشعار لإدارات البوتات الأخرى حتى يقرر كل بوت نشره أو رفضه.

### الخطوة الثانية — نوع المنتج

اختر واحداً من:

- **🔑 كود**
- **📧 إيميل وباسورد**
- **📝 منتج حر**
- **👥 حساب مشترك**
- **🛠 تنفيذ خدمة**

تنفيذ الخدمة **ليس محلياً إجبارياً**: إذا اخترت محلي تبقى في هذا البوت، وإذا اخترت عام تدخل شبكة المنتجات وتصل لبقية الإدارات للموافقة والتسعير.

### الخطوات التالية

البوت يطلب بالترتيب:

1. الاسم.
2. السعر.
3. الوصف أو `-` بدون وصف.
4. الضمان أو `-` بدون ضمان.
5. الصورة أو `-` بدون صورة.
6. إذا كان المنتج «تنفيذ خدمة»، يطلب نوع بيانات الزبون ثم رسالة التعليمات.
7. إذا كان منتج مخزون، ينتقل مباشرة لإضافة المخزون.

## 3) المنتج أو الخدمة العامة: التسعير ثم التأكيد

عندما ينشر أحمد منتجاً أو خدمة عامة، يصل إشعار إلى إدارات البوتات الأخرى باسم صاحب المنتج والسعر الأساسي والتفاصيل. تحت الإشعار يوجد:

- **💵 تسعير المنتج/الخدمة**
- **❌ رفض في هذا البوت فقط**

إذا اختار الأدمن **تسعير**:

1. يرسل سعر البيع المحلي، ويجب ألا يقل عن سعر صاحب المنتج.
2. يعرض البوت ملخصاً قبل النشر: **سعر صاحب المنتج + سعر البيع في بوتك + ربح بوتك**.
3. تظهر زران: **✅ نعم، نشر في بوتي** أو **❌ لا، رفض وإخفاء**.
4. عند النشر تصبح الحالة `published` فعلياً، ويرسل البوت إشعاراً للمستخدمين الموجودين في هذا البوت.
5. عند الرفض تصبح الحالة `rejected` في هذا البوت فقط ولا تتأثر بقية البوتات.

الرفض **قابل للتراجع**. افتح لاحقاً: **إدارة المنتجات المضافة → المنتج/الخدمة → النشر والظهور → تسعير ونشر من جديد**.

مثال الربح: أحمد يضع سعر الخدمة الأساسي `1$`، وأنت تسعرها `3$`. عند البيع يكون **حق أحمد 1$** و**ربح بوتك 2$**. النظام يسجل حق صاحب الخدمة في حسابات الشبكة، وعند استرداد الخدمة يعكس هذا الاستحقاق حتى تبقى الحسابات صحيحة.

قرار النشر/الرفض النهائي محمي داخل Database Transaction حتى لا يتعارض أدمنان إذا ضغطا في الوقت نفسه.

## 4) المنتج المحلي: إظهاره أو إخفاؤه من البوتات الأخرى

افتح **إدارة المنتجات المضافة** ثم اختر المنتج المحلي ثم **🌐 النشر والظهور**.

- **🌐 إظهار المنتج في البوتات الأخرى** يحول المنتج المحلي إلى عام وينقل مخزونه غير المباع بأمان إلى الشبكة.
- بعد أن يصبح عاماً يظهر لصاحب المنتج زر **🙈 إخفاء من البوتات الأخرى**.
- عند الإخفاء يبقى المنتج ومخزونه داخل بوت صاحبه ولا يتم حذف البيانات.
- يمكن إعادة تشغيل التوزيع من **🌐 إظهار في البوتات الأخرى** بأي وقت.
- كل بوت آخر يحتفظ بقراره المحلي: نشر، إخفاء، حذف محلي أو رفض.
- نفس المبدأ ينطبق على **تنفيذ الخدمة**: يمكن لصاحبها إبقاؤها محلية أو جعلها عامة، ثم إيقاف توزيعها وإعادته لاحقاً بدون حذف سجلاتها.

إذا كان هناك مخزون مخفي أو حساب مشترك مستخدم جزئياً، يمنع البوت التحويل إلى عام حتى تتم معالجة المخزون، وذلك لمنع فقدان أو تكرار المحتوى.

## 5) إدارة المنتج بدون زحمة أزرار

من **إدارة المنتجات المضافة** افتح أي منتج. الصفحة الرئيسية للمنتج أصبحت مقسمة إلى أقسام:

- **✏️ تعديل بيانات المنتج**
- **🔐 إدارة وكشف المخزون والكودات**
- **📊 المخزون والمبيعات والمساهمون**
- **🌐 النشر والظهور**
- **🗑 حذف المنتج نهائياً**

الخدمة التنفيذية لا تعرض أزرار المخزون لأنها لا تحتاج مخزوناً.

## 6) تعديل بيانات المنتج

من **✏️ تعديل بيانات المنتج** تستطيع تعديل الاسم والسعر والوصف والضمان والصورة.

إذا كان المنتج عاماً من متجر آخر، تظهر لك تخصيصات **هذا البوت فقط** حتى لا تغيّر نسخة صاحب المنتج أو بقية البوتات.

إذا كنت صاحب المنتج العام، تظهر لك أيضاً الحقول العامة التي تؤثر على الكتالوج المشترك.

تغيير نوع المنتج بين كود/حساب/منتج حر/مشترك يُمنع إذا بقي مخزون غير مباع، بما فيه المخزون المخفي. الهدف هو منع تفسير الأكواد القديمة بصيغة خاطئة بعد تغيير النوع.

## 7) كشف وتعديل وحذف وإخفاء الكود أو المخزون

افتح المنتج ثم **🔐 إدارة وكشف المخزون والكودات**.

كل عنصر يعرض رقمه وحالته والمتبقي منه. افتح العنصر لتجد:

- **كشف المحتوى المحفوظ** للإدارة.
- **✏️ تعديل المحتوى** إذا لم يُبع أو يُستخدم.
- **🙈 إخفاء من البيع** مع بقاء المحتوى محفوظاً.
- **👁 إظهار للبيع** لإرجاع العنصر المخفي.
- **🗑 حذف عنصر المخزون** إذا لم يُبع أو يُستخدم.

إذا كان العنصر قد تم تسليمه أو استُخدم، يبقى قابلاً للكشف للإدارة لكن يمنع تعديله أو حذفه حفاظاً على سجل التسليم.

المخزون المخفي لا يدخل في العدد المتاح ولا يتم تسليمه للزبائن.

## 8) حذف واستعادة المنتج

زر **🗑 حذف المنتج نهائياً** لا يمسح سجل المنتج من PostgreSQL. بدلاً من ذلك:

- يوقف البيع.
- يبقي الطلبات.
- يبقي سجل التسليم.
- يبقي المخزون.
- يسمح بالاستعادة لاحقاً من **🌐 النشر والظهور**.

هذا مقصود لأن قاعدة البيانات مرتبطة بالإنتاج ولا نريد فقدان تاريخ مهم.

## 9) الأدمنات: إضافة وحذف

`/admin` ← **إعدادات المتجر** ← **👑 إدارة الأدمنات**.

- **➕ تعيين أدمن بالـ Telegram ID**: أرسل ID رقمي، ويحصل على صلاحية الإدارة فوراً.
- **➖ حذف صلاحية الأدمن**: يسحب صلاحية `/admin` من ذلك الشخص.

الأدمن المضاف يستطيع إدارة المنتجات والمخزون والمستخدمين والأرصدة وطرق الدفع والإعدادات وAPI مزودي الأرقام الافتراضية والإيموجيات.

يوجد حماية تمنع حذف الأدمن الأساسي المحمي أو ترك البوت بدون إدارة.

## 10) الرصيد: إضافة وخصم وتصفير

`/admin` ← **العملاء والتواصل**.

- **➕ إضافة رصيد**
- **➖ خصم رصيد**
- عند فتح حساب مستخدم يظهر أيضاً **🧹 تصفير المحفظة**.

الخصم لا يسمح بجعل الرصيد سالباً، وتسجل العملية في `BalanceTransactions` مع الأدمن الذي نفذها.

## 11) مركز طرق الدفع الذكي

`/admin` ← **الدفع والمحفظة** ← **💳 مركز طرق الدفع**.

تم جمع كل طرق الدفع في مكان واحد، بما فيها:

- Binance المحلي أو الموروث من الشبكة.
- SuperQi المحلي أو الموروث.
- طرق الدفع التي يضيفها هذا البوت.
- طرق الدفع المشتركة القادمة من البوتات الأخرى.

من قائمة واحدة يقدر الأدمن يعرف حالة كل طريقة ومصدرها ولون زرها. ومن داخل أي طريقة يقدر:

- **👁 إظهار / 🙈 إخفاء** الطريقة في هذا البوت فقط.
- **⬆️ رفع / ⬇️ تنزيل** لتغيير ترتيب ظهورها للزبون.
- **🎨 تغيير لون الزر**: افتراضي، أزرق، أخضر، أحمر.
- تعديل الاسم، الإيموجي، رقم الدفع، العملة، سعر الصرف والحد الأدنى إذا كانت الطريقة ملك هذا البوت.
- تشغيل/إيقاف الطريقة العامة إذا كانت مملوكة لهذا البوت.
- حذف الطريقة المحلية مع إبقاء سجلات العمليات القديمة.

طرق الدفع التابعة لبوت آخر لا يسمح النظام بتعديل بياناتها أو حذفها من صاحبها؛ الأدمن يقدر فقط يخفيها أو يظهرها أو يغير ترتيبها ولونها داخل بوتِه.

يوجد أيضاً زر **👁 إظهار الكل** وزر **↕️ ترتيب افتراضي** حتى يرجع الأدمن بسرعة لو لخبط الترتيب.

إعداد Binance ورقم SuperQi انتقلا إلى مركز طرق الدفع حتى لا تبقى إعدادات الدفع موزعة بأكثر من مكان.

ترتيب الطرق، إخفاؤها وألوانها تُحفظ في PostgreSQL داخل إعداد `payment_ui_preferences` بدون حذف أو إعادة إنشاء أي جدول.

## 12) API الأرقام الافتراضية

`/admin` ← **إعدادات المتجر** ← **📱 مزودات الأرقام الافتراضية**.

لكل مزود يستطيع الأدمن:

- إضافة/تغيير API.
- حذف API.
- تعديل الربح.
- فحص المزود.

المزودات الموجودة: SMSBower وSMS-Man وGrizzlySMS. المفاتيح الحساسة تحفظ في `SecureSetting` ولا تعرض كنص كامل بعد الحفظ.

## 13) أزواج الإدارة الموجودة في v12

| المجال | الإجراء الموجب | الإجراء العكسي |
|---|---|---|
| الأدمن | تعيين أدمن | حذف صلاحية الأدمن |
| الرصيد | إضافة رصيد | خصم رصيد / تصفير |
| المنتج | إضافة/إظهار | إخفاء/أرشفة |
| توزيع المنتج | إظهار في البوتات الأخرى | إخفاء من البوتات الأخرى |
| المخزون | إضافة/إظهار | إخفاء/حذف غير المستخدم |
| طريقة الدفع | إضافة/إظهار | إخفاء/حذف |
| ترتيب الدفع | رفع | تنزيل |
| المتجر | فتح | إغلاق |
| الإشعارات | تشغيل | إيقاف |
| API المزود | إضافة/تغيير | حذف |

## 14) حماية قاعدة البيانات

v12 لا يستخدم `DROP TABLE` ولا `TRUNCATE` ولا `sequelize.sync({ force: true })` لمسح بيانات الإنتاج.

التغييرات الجديدة في قاعدة البيانات إضافية:

- `Merchants.networkDistributionEnabled` للتحكم بتوزيع المنتج على البوتات الأخرى.
- `Codes.isHidden` لإخفاء كود/حساب من البيع بدون حذفه.

وتبقى الجداول والبيانات السابقة مثل المستخدمين والأرصدة والطلبات والمنتجات والتسليمات والمخزون والإعدادات محفوظة.

## 15) التشغيل

المصدر الحقيقي داخل `src/`، وملفات JavaScript في جذر المشروع مجرد ملفات توافق حتى أوامر Railway القديمة تبقى شغالة.

- التشغيل: `npm start`
- فحص Syntax: `npm run check`


## 13) كشف الأرصدة ومصدر كل شحن

من `/admin` افتح **العملاء والتواصل → 💰 كشف الأرصدة ومصادر الشحن**.

- تظهر فقط الحسابات التي رصيدها الحالي أكبر من صفر.
- الترتيب من أعلى رصيد إلى الأقل.
- يظهر إجمالي الرصيد الموجود حالياً في البوت.
- افتح أي مستخدم لرؤية: مبلغ كل حركة، هل هي إضافة أو خصم، المصدر (إدارة/Binance/SuperQi/طريقة محلية/طريقة مشتركة/إحالة/استرجاع/شراء)، وقت العملية، المرجع، والأدمن المنفذ عندما يكون مسجلاً.

لشحن أو خصم أو تصفير رصيد شخص استخدم **Telegram ID** أو **@username**. الاسم العادي غير معتمد حتى لا يتم اختيار شخص مشابه بالاسم.

## 14) الحذف النهائي

- زر **حذف المنتج نهائياً** يزيل سجل المنتج ومخزونه وطلبات التشغيل المرتبطة به من قاعدة التشغيل. لا يوجد استرجاع من أرشيف بعده.
- إذا تريد فقط إيقاف المنتج عن البيع، استخدم **إخفاء**.
- المنتج العام الذي يملكه شريك لا يمكن حذفه من طرف متجر آخر؛ المتجر الآخر يستطيع رفضه أو إخفاءه فقط.
- إذا كان للمنتج مخزون مملوك لشركاء آخرين، يمنع الحذف إلى أن تتم معالجة ذلك المخزون حتى لا تضيع حقوق الشركاء.
- حذف الأدمن غير المحمي أصبح حذفاً فعلياً من سجل الأدمنات.

## 15) شركاء API

من **الشبكة → API والشركاء** افتح أي شريك:

- **⛔ إيقاف API**: يمنع مفتاح الشريك فوراً.
- **✅ إعادة تفعيل API**: يعيد السماح له فوراً.
- **🗑 حذف الشريك نهائياً**: يحذف تسجيل الشريك ومفتاحه من البوت الرئيسي.

ملاحظة: حذف تسجيل الشريك لا يحذف Schema/قاعدة بيانات بوت الشريك المنفصلة تلقائياً، لأن ذلك قد يمسح مستخدمينه وطلباته. إذا أردت حذف قاعدة بياناته أيضاً يجب أن تكون عملية منفصلة ومقصودة.

## v15 — ضمان جمناي وإشعار الدفعات

- ضمان **جمناي 18 شهر** ثابت **24H (24 ساعة)** ولا يظهر زر تعديل الضمان لهذا المنتج.
- عند استيراد روابط القناة، الإدارة ترى عداداً مثل `17/50`.
- لا يصل أي إشعار مخزون للمستخدمين قبل اكتمال 50 رابط جديد فعلياً.
- عند اكتمال 50 يبدأ العداد من الصفر للدفعة التالية، وكل 50 رابط جديدة تطلق إشعاراً جديداً.
- المكرر لا يُحسب ضمن الخمسين.

## إصلاح v15.0.1 — ربط قناة المخزون

- إصلاح قراءة معرّف المنتج في أزرار ربط/فحص/فك ربط قناة Gemini؛ لم يعد يتحول إلى `NaN`.
- إضافة تحقق يمنع أي معرّف غير صالح من الوصول إلى استعلام PostgreSQL.
- انتظار مسار إدارة الأدمن داخل معالج الأزرار حتى تلتقط طبقة الحماية أي خطأ غير متوقع بدل إسقاط عملية البوت.


## إصلاح v15.0.2 — تقرير مخزون القناة

- تقرير إضافة روابط **جمناي 18 شهر** يُرسل الآن داخل قناة المخزون نفسها بعد معالجة الرابط، وليس إلى محادثات الأدمن.
- يحتاج البوت صلاحية **Post Messages** بالإضافة إلى **Delete Messages** في قناة المخزون.
- إذا فشل النشر داخل القناة، يصل للأدمن تنبيه خطأ فقط حتى يعرف أن صلاحية النشر ناقصة.

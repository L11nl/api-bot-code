# دليل الأدمن — CD Store v9

هذا الدليل يشرح إدارة كل بوت خطوة بخطوة. كل أدمن مضاف من لوحة البوت يمتلك **صلاحيات كاملة داخل ذلك البوت**، ولا يحتاج إضافة ID جديد إلى Railway بعد أول تشغيل.

## 1) إضافة أدمن جديد بالـ Telegram ID

1. افتح `/admin`.
2. ادخل **إعدادات المتجر**.
3. اختر **👑 إدارة الأدمنات**.
4. اضغط **➕ إضافة أدمن بالـ Telegram ID**.
5. أرسل ID رقمي صحيح، مثال: `123456789`.
6. بمجرد الحفظ، يصبح الشخص أدمن كامل الصلاحيات في هذا البوت.

الأدمن الجديد يستطيع إدارة المنتجات والمخزون والعملاء وطرق الدفع والإعدادات وAPI مزودي الأرقام الافتراضية. الأدمن الأساسي الموجود أولاً في `ADMIN_IDS` يبقى محمياً حتى لا يفقد البوت كل المدراء بالخطأ.

## 2) حذف أدمن

من **إدارة الأدمنات** اضغط حذف بجانب الـID ثم أكد العملية. لا يمكن للأدمن حذف نفسه، ولا يمكن حذف الأدمن الأساسي المحمي، ولا يمكن ترك البوت بدون أي أدمن.

## 3) إضافة منتج: خاص أو عام

من **المنتجات والمخزون > إضافة منتج** يظهر أول سؤال:

- **🔒 محلي**: المنتج يبقى داخل هذا البوت فقط.
- **🌐 عام**: المنتج يدخل كتالوج الشبكة ويصل إشعار إلى إدارات البوتات الأخرى.

عند اختيار **عام** يصل للأدمنات في كل بوت آخر إشعار يحتوي الاسم والنوع وسعر صاحب المنتج والوصف والضمان. كل بوت يتخذ قراره بشكل مستقل:

- **💵 تسعير المنتج ونشره**: يحدد أدمن ذلك البوت سعره المحلي، بشرط ألا يقل عن سعر صاحب المنتج، ثم يُحفظ المنتج بحالة `published` فعلياً داخل قاعدة بيانات ذلك البوت.
- **❌ رفض**: يرفض المنتج في ذلك البوت فقط ولا يتأثر باقي البوتات.

تم حماية الموافقة/الرفض بـ Database Transaction لمنع تعارض قرار أدمنين في اللحظة نفسها.

## 4) تعديل منتج عام داخل بوتك فقط

حتى لا يغير أدمن بوت واحد نسخة الجميع، المنتجات العامة تدعم تخصيصات محلية مستقلة:

- الاسم داخل هذا البوت فقط.
- السعر داخل هذا البوت فقط.
- الوصف داخل هذا البوت فقط.
- الضمان داخل هذا البوت فقط.
- الصورة داخل هذا البوت فقط.
- إخفاء أو حذف محلياً من هذا البوت.

استخدم `-` داخل حقول التخصيص المحلي للرجوع إلى القيمة العامة. في الصورة المحلية اكتب `حذف` إذا تريد عدم عرض أي صورة في هذا البوت فقط.

صاحب المنتج الأصلي يبقى عنده أيضاً أزرار تعديل البيانات **العامة** لكل الشبكة.

## 5) ربط API مزودي الأرقام الافتراضية

1. افتح `/admin`.
2. ادخل إعدادات المتجر أو قسم الأرقام الافتراضية.
3. افتح **مزودات الأرقام الافتراضية**.
4. اختر المزود.
5. اضغط إضافة/تغيير API Key وأرسل المفتاح.

كل أدمن مضاف لهذا البوت يستطيع تنفيذ هذه الخطوات. المفاتيح تُخزن في `SecureSetting` بشكل مشفر ولا يتم عرض المفتاح بعد الحفظ.

المزودات الموجودة في هذا الإصدار: SMSBower و SMS-Man و GrizzlySMS.

## 6) ترتيب طرق الدفع

1. افتح `/admin`.
2. ادخل **الدفع والمحفظة**.
3. افتح **إدارة طرق الدفع**.
4. افتح طريقة الدفع المطلوبة.
5. استخدم **⬆️ رفع للأعلى** أو **⬇️ تنزيل للأسفل**.

الترتيب يُحفظ في PostgreSQL عبر `sortOrder`. بعد أي إضافة أو تعديل أو نقل أو حذف يتم مسح كاش طرق الدفع فوراً حتى يظهر الترتيب الجديد للمستخدمين.

## 7) حماية قاعدة البيانات الحالية

التحديث لا يحذف الجداول الحالية ولا يعمل Reset للبيانات. تمت إضافة الحقول/الجداول الجديدة بطريقة غير مدمرة:

- جدول `BotAdmins` لحفظ الأدمنات بصورة دائمة.
- حقل `Merchants.localContentOverride` لحفظ تخصيص وصف/ضمان/صورة المنتج داخل كل بوت.

تبقى بيانات المستخدمين والأرصدة والمنتجات والمخزون والطلبات والتسليمات والإعدادات الحالية كما هي. قبل أي نشر إنتاجي كبير يبقى أخذ Backup من PostgreSQL ممارسة موصى بها دائماً.

## ملاحظات تشغيل

- `ADMIN_IDS` في Railway أصبح **Bootstrap/Safety Admin** وليس الطريقة الوحيدة لإضافة الأدمنات.
- تشغيل المشروع الرسمي: `npm start` ويشغل `src/index.js`.
- ملفات JavaScript في جذر المشروع أصبحت ملفات توافق صغيرة تحول إلى `src/`، حتى لا توجد نسختان مختلفتان من منطق البوت.
- نفذ `npm run check` بعد كل تعديل برمجي للتأكد من سلامة Syntax للملفات الأساسية.

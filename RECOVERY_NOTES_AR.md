# ملاحظات الاستعادة الآمنة

هذه الحزمة مبنية على آخر نسخة كاملة قبل الاستبدالات الكبيرة: `ca585b3`.

## ما تم الحفاظ عليه

- ملفات البوت وقاعدة البيانات والطلبات والشبكة والدفع القديمة كاملة.
- جداول وحقول المنتجات والمخزون والطلبات والمحفظة والشبكة.
- دقة أرصدة المحافظ `NUMERIC(18,8)`.
- ترحيلات قاعدة البيانات غير الهدامة وتحويل البيانات القديمة دون حذفها.

## ما تم دمجه

- مزودو الأرقام `SMSBower` و`SMS-MAN` و`GrizzlySMS` بشكل مستقل.
- API وربح منفصلان لكل مزود، مع حفظ المفاتيح مشفّرة.
- مهلة انتظار SMS مقدارها 10 دقائق.
- إضافة `providerId` و`profitUsd` إلى طلبات الأرقام القديمة بطريقة غير هدامة.
- إنشاء فهارس طلبات الأرقام بعد إضافة الأعمدة، لمنع خطأ Railway رقم `42703`.
- الإبقاء على محاسبة تكلفة المزود والاسترداد بين المتاجر.
- تنظيف مدخل مفتاح SMSBower/SMS-MAN/GrizzlySMS من الروابط والبادئات والمسافات الخفية قبل اختباره، من دون حفظ مفتاح يرفضه المزود أو إرساله إلى مزود آخر.
- إضافة قاموس Telegram Premium ثنائي اللغة لكل المعرفات المرسلة من المالك، وتطبيقه مركزياً على المنتجات والخدمات والأزرار ورسائل HTML.
- إضافة شاشة مالك فقط لإنشاء ربط عربي، وترجمة اسمه الإنجليزي تلقائياً، وحفظ الربط في `Setting` دون تعديل جداول المنتجات أو حذف بيانات قديمة.
- مطابقة المنتجات القديمة وقت العرض، لذلك لا توجد عملية ترحيل تعيد كتابة أو تختصر أوصاف المنتجات السابقة.
- تصحيح معرّفات المنصات المحفوظة خطأ للمنتجات القديمة عند التشغيل: تُحدّث فقط `nameEmojiId` و`nameEmojiAlt` و`nameArHtml`، مع إبقاء بقية JSON كما هو.
- أبقى قاموس Canva وCapCut وNetflix وGemini وبقية المنصات كقيم افتراضية، مع السماح للمالك بتغيير ربط الخدمة نفسها. المطابقة الأكثر تحديداً تفوز حتى لا تستبدل كلمة عامة إيموجي الخدمة الصحيحة.
- أعاد كل منتج إلى زر واحد كامل، ويعرض معرفي المال والصندوق Premium داخل تفاصيل المنتج، وأخفى الوردة والهاتف القديمين دون تعديل بيانات المنتج أو المخزون.
- حذف الإيموجيات العادية مركزياً من واجهة البوت، مع إبقاء الضروري للتنقل أو التحذير والخطر فقط عندما لا يتوفر بديل Premium.
- إضافة محرر مركزي آمن للنصوص والأزرار: بحث تقريبي، تأكيد للنتيجة الكاملة، ثم استبدال النص وإيموجيه المميز مع حفظه في `Setting`.
- إبقاء `callback_data` وأسماء أزرار لوحة المفاتيح الداخلية دون تغيير، حتى لا يؤدي تغيير الاسم الظاهر إلى تعطيل الزر.
- إضافة تراجع مستقل يعيد النص الأصلي فقط، وفهرس بحث محدود يستبعد المفاتيح وكلمات المرور وبيانات التسليم الحساسة.
- إضافة مطابقة هوية خاصة بالمنتجات تمنع `مشترك` أو `جوجل` من التغلب على CapCut أو Gmail، مع احترام التعديل اليدوي الذي ينتمي إلى نفس المنصة.
- إضافة مراجعة مالك شاملة تصحح حقول إيموجي المنتجات فقط، وتطبيق دلالي مركزي على الرسائل والتعليقات والأزرار بحسب صياغة السطر.
- عزل روابط الإيموجي القديمة غير المؤكدة عن هوية Canva وCapCut وGmail وبقية المنصات، مع السماح بأي استبدال جديد يحفظه المالك عمداً.
- قفل إيموجي زر المنتج على الخدمة الفعلية حتى لا يستطيع تعديل نص قديم تحويل Canva إلى YouTube أو CapCut إلى حفظ.
- إصلاح توجيه أزرار لوحة الرد بعد إزالة الإيموجي العادي، بإرجاع النص الظاهر تلقائياً إلى الأمر الداخلي الأصلي أثناء الضغط وبعد إعادة التشغيل.
- إضافة ترحيل إعدادات لمرة واحدة يعيد معرّفات قاموس المالك النهائية من دون حذف أي سجل تجاري.
- إضافة قراءة طبقات الأسعار الكاملة لمزودي الأرقام مع رجوع آمن إلى الواجهة القديمة، وقائمة شاملة لأرخص الخدمات المتاحة.
- رفع دقة سعر الرقم الافتراضي والعرض وتوقيع زر الشراء والاسترجاع إلى أربع منازل عشرية، من دون تغيير هامش الربح أو أرصدة قاعدة البيانات القديمة.
- إضافة مزامنة قاموس الإيموجيات المميزة من البوت الرئيسي إلى جميع بوتات الشبكة عبر API مصادق عليه، مع نسخة محلية آمنة عند انقطاع الشبكة.
- إصلاح تعارضات هوية المنصات المحفوظة، ومنها Canva المرتبط بمعرّف YouTube، من دون منع المالك من اختيار معرّف Premium جديد غير تابع لخدمة أخرى.
- في الإصدارات القديمة كانت الخدمة محلية فقط. في v12 بقيت الخدمات القديمة آمنة ومحلية افتراضياً، لكن يمكن لصاحبها جعلها عامة يدوياً، والخدمات الجديدة تسمح باختيار **محلي/عام** وتدخل كتالوج الشبكة عند اختيار عام.
- تحسين تشخيص البوت الثاني في Railway: قبول روابط PostgreSQL البديلة ومتغيرات `PG*`، وإظهار تعليمات مباشرة لإضافة مرجع `DATABASE_URL` إلى خدمة البوت الجديدة.
- تبسيط إنشاء بوت الشريك إلى ثلاث خطوات، والتحقق من التوكن وحذف رسالته فوراً، ثم توليد جميع متغيرات Railway والشبكة وSchema في بلوك واحد من دون حفظ التوكن أو طلب اختيار عملة.

## قبل النشر

1. أنشئ Backup/Snapshot لقاعدة PostgreSQL الحالية في Railway.
2. لا تستبدل قاعدة البيانات ولا تغيّر `DATABASE_URL` أو `DATABASE_SCHEMA`.
3. ارفع المشروع كاملاً ثم راقب `/ready` وسجل النشر.
4. اختبر تسجيل الدخول، المنتجات، المخزون، المحفظة، الطلبات، ثم شراء رقم تجريبي صغير.

> الكود يحافظ على البيانات الموجودة ويستعيد الوظائف والبنية القديمة. أما السجلات التي حُذفت فعلياً من PostgreSQL في نشر سابق فلا يمكن للكود إنشاؤها من جديد؛ يجب استرجاعها من Backup/Snapshot لقاعدة البيانات.


## تحديث v12 — الخدمات العامة والتسعير بين البوتات

- كل أنواع المنتجات، بما فيها **تنفيذ خدمة**، تدعم اختيار محلي أو عام.
- المنتج/الخدمة العامة في البوت الآخر تمر بمسار تسعير ثم تأكيد صريح قبل النشر، أو رفض وإخفاء محلي قابل للتراجع لاحقاً.
- عند النشر في بوت جديد يصل إشعار للمستخدمين في ذلك البوت.
- الخدمات العامة تستخدم دفتر الشبكة الموجود أصلاً لتسجيل حق صاحب الخدمة بالسعر الأساسي وربح البوت البائع من فرق السعر؛ لم تتم إضافة Reset أو حذف بيانات.
- استرداد الخدمة العامة يعكس استحقاق صاحب الخدمة محاسبياً، مع إعادة المحاولة تلقائياً إذا كانت الشبكة الرئيسية غير متاحة مؤقتاً.
- لم تُضف أوامر `DROP TABLE` أو `TRUNCATE` أو `sequelize.sync({ force: true })`.


## v13 — ملاحظات الاستعادة
- أضيف عمود `BalanceTransactions.completedAt` بطريقة add-if-missing فقط.
- لا يوجد `DROP TABLE` أو `TRUNCATE` في ترقية v13.
- حذف منتج من زر الحذف بعد v13 هو حذف دائم ومقصود؛ لا تعتمد على الأرشيف لاستعادته.
- حذف شريك API يزيل صف `NetworkClient` فقط ولا يسقط Schema الشريك المنفصل.

## إضافات v14 — جمناي 18 شهر

- إنشاء/اعتماد منتج `جمناي 18 شهر` بمعرف شبكة ثابت، مع تجنب إنشاء نسخة ثانية إذا كان نفس المنتج موجوداً مسبقاً في البوت الرئيسي.
- إضافة حقول سعر مورد الشبكة، شرائح تسعير الكمية، نطاق الخصم، والحد الأعلى للشراء عبر `addColumnIfMissing` فقط؛ لا يوجد حذف جداول أو إعادة ضبط للبيانات.
- خصومات البوت الرئيسي: 1–9 = $0.50، 10–49 = $0.40، 50–99 = $0.35، 100–499 = $0.30، 500–1000 = $0.25. لا تظهر شريحة لا يستطيع المخزون الحالي تغطية حدها الأدنى.
- بوتات الشركاء لا تستخدم خصم الكمية: تكلفة المصدر $0.40، الحد الأدنى للبيع $0.50، والربح الافتراضي $0.10 لكل كود، مع السماح برفع سعر البيع المحلي.
- قناة المخزون تحفظ فقط روابط `serviceactivation.google.com/subscription/new/` بعد التحقق ومنع التكرار، ثم تحذف منشور القناة بعد نجاح المعالجة. فشل قاعدة البيانات لا يحذف المنشور.
- Telegram Bot API لا يوفر مسحاً لتاريخ القناة القديم؛ الاستيراد يبدأ بالمنشورات الجديدة التي تصل بعد ربط القناة وإضافة البوت إليها.


## v15
- Gemini 18 شهر: ضمان ثابت 24H (24 ساعة).
- إشعارات المخزون للمستخدمين تُرسل كل 50 رابط جديد فعلياً فقط، والعداد محفوظ في PostgreSQL.
- الروابط المكررة لا تدخل في عداد الإشعار.
